<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="spring_test.dao.UserDao">
    <!-- 根据用户名和密码查询用户-->
    <select id="findUserByUsernameAndPassword" 
        parameterType="spring_test.entity.User"
        resultType="spring_test.entity.User">
     select * from fuser where username =#{username} and password=#{password} and rownum=1 
    </select>
    
</mapper>